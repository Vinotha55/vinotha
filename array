#include<stdio.h>
int main()
{
    int [3][5],i,j,sum[3];
    for(i=0;i<3;i++)
    {
        sum[i]=0;
        printf("Enter marks of a student%d:\n",i*1);
        for(j=0;j<5;j++)
        {
            scanf("%d",&a[i][j]);
            sum[i]+=a[i][j];
        }
    }
    
    for(i=0;i<3;i++)
    {
        for(j=0;j<5;j++)
    
    }
}
